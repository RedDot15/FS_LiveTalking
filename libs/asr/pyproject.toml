[project]
name = "asr"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "realtrevor15", email = "ttkien1503@gmail.com" }
]
requires-python = ">=3.10"
dependencies = [
    "librosa>=0.11.0",
    "realistic",
]

[build-system]
requires = ["uv_build>=0.8.0,<0.9"]
build-backend = "uv_build"

[tool.uv.sources]
realistic = { workspace = true }
