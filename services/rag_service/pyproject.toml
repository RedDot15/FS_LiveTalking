[project]
name = "rag-service"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "realtrevor15", email = "ttkien1503@gmail.com" }
]
requires-python = ">=3.10"
dependencies = [
    "asgi-correlation-id>=4.3.4",
    "base",
    "fastapi>=0.116.1",
    "logger",
    "pydantic-settings>=2.10.1",
]

[project.scripts]
rag_service = "rag_service:main"

[build-system]
requires = ["uv_build>=0.8.7,<0.9.0"]
build-backend = "uv_build"

[tool.uv.sources]
chromadb-client = { workspace = true }
logger = { workspace = true }
base = { workspace = true }

